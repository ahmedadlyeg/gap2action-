import { PlaceholderPage } from '@/components/shared/PlaceholderPage';
export function Gaps() {
  return <PlaceholderPage title="Gap Analysis" description="Visual gap matrix comparing actual vs. target scores per section. Severity colour-coding: red (High) → amber (Medium) → green (Low)." />;
}
